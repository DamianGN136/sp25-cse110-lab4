1. The values of num1 and num2 are strings and are being concatenated. Thus, the output for my test values num1 = 3, num2=4 is '34'. The sum should be 7
2. To fix this I would just type cast both num values as such: Number(num1) + Number(num2)
